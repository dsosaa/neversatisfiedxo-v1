# Local Development Environment Variables
# Copy this file to .env.local and customize as needed

# Database Configuration
POSTGRES_USER=mediacms
POSTGRES_PASSWORD=devpassword123
POSTGRES_DB=mediacms

# Redis Configuration
REDIS_PASSWORD=devpassword123

# Django Configuration
DJANGO_SECRET_KEY=dev-secret-key-for-local-testing-only-change-in-production
DJANGO_DEBUG=True

# Application Configuration
NEXT_PUBLIC_SITE_NAME=neversatisfiedxo
NEXT_PUBLIC_BASE_URL=http://localhost:3000
MEDIACMS_BASE_URL=http://mediacms:80

# Authentication
GATE_PASSWORD=devpassword123
JWT_SECRET=dev-jwt-secret-for-local-testing

# Cloudflare Stream (Optional - for video playback)
NEXT_PUBLIC_CF_STREAM_CUSTOMER_CODE=your_cf_stream_customer_code
CF_ACCOUNT_ID=your_cf_account_id
CF_STREAM_API_TOKEN=your_cf_stream_api_token
MEDIACMS_API_TOKEN=your_mediacms_api_token

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://web:3000,https://videos.neversatisfiedxo.com

# Development Optimizations
NEXT_PUBLIC_DEBUG=true
NEXT_PUBLIC_OPTIMIZE_IMAGES=true
NEXT_PUBLIC_LOW_QUALITY_MODE=true

